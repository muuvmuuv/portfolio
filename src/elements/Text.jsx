import React from 'react'

const Emphasized = (props) => <em {...props} />

const Strong = (props) => <strong {...props} />

const Delete = (props) => <del {...props} />

export { Emphasized, Strong, Delete }
