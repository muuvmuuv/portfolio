# https://github.com/Arkweid/lefthook/blob/master/docs/full_guide.md

pre-commit:
  commands:
    check:
      glob: "*.{js,ts,cjs,mjs,jsx,tsx,json,jsonc}"
      run: node_modules/.bin/biome check --no-errors-on-unmatched {staged_files}

commit-msg:
  commands:
    lint-commit-msg:
      run: node_modules/.bin/commitlint -x "@commitlint/config-conventional" --edit
